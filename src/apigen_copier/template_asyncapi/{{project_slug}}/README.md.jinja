# {{ project_slug }} — README
This project was auto-generated from an AsyncAPI v3 specification.

## Quick Start
```bash
# Install dependencies
pip install uv
uv sync

# Start the service
faststream run src.main:app
```

## Docker
```bash
docker compose up --build
```
